# convert_video_audio
This repository contains a Python script that uses the moviepy library to convert videos into audio files. The script is easy to use and can be easily integrated into projects that require video-to-audio conversion, such as converting to MP3 format.
